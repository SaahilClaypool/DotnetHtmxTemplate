@model List<Note>
<h1>Notes</h1>
create
<partial name="_note" model="default(Note)"/>

<div id="note-container" class="border border-black">
    @foreach(var note in Model)
    {
        <partial name="_viewNote" model="note"/>
    }
</div>